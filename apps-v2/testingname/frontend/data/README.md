# Order management tool — sample data

This directory was seeded by the onboarding flow. The agent treats these JSON files as the initial data source; the app starts up running against them. When you connect a real data source, the agent will rewrite the queries to point at it.

## Files

- `orders.json`
